import { StatusMappingPipe } from './status-mapping.pipe';

describe('StatusMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
